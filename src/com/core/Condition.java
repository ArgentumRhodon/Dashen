package com.core;

/*
Due to time constrains, this isn't used so much.
But this is a simple interface that allows for
    quick definitions of boolean checks.
 */
public interface Condition {

    boolean isMet();

}
